namespace ManejoPresupuesto.Models
{
    /**tipo de dato especial que se utiliza para declarar un conjunto de constantes enteras. 
     * Las constantes de un enum se denominan elementos, y cada uno tiene un valor entero asignado por defecto, comenzando en 0 
     * y continuando en incrementos de 1.*/
    public enum TipoOperacion
    {
        Ingreso = 1,
        Gasto = 2,
    }
}
